---
name: tech-ideas-daily
description: 毎日1回実行し、エンジニアの好奇心を刺激する「技術知見カード」を3枚生成してMarkdownに記録するスキル。無名の仕様・目的外利用・サイドチャネルなど“自分の発想の外”にある尖った技術と、それを無理やり活かす奇抜なアイデアを、凡庸さを排して出し続ける。日次ファイルとインデックスに追記する。技術ネタの定期収集・アイデア出し・知見の蓄積をしたいときに使う。
---

# 技術知見デイリー

## これは何 / 何を出すか
- **役割**: 毎日1回実行され、3枚の「技術知見カード」を作って記録する。実行内容は毎日まったく同じ（このSKILL）。変わるのは中身だけ。
- **狙い**: 「わざわざそんなことする？」と「そんな手があったか！」の両方を満たす、自分の発想の外にある尖ったネタを出し続ける。**実用性を最大化すると凡庸になる**ので、わざと最頻解（ありがちな第一感）を捨てる。
- **各カード = 1つの独立したアイデア**。4ブロック（事実 / 何ができる / 技術の魅力 / 本来どこで使われてるか）＋🔎検索ワード。
- 3枚は**それぞれ違う「move（発想の技）」**を使い、なるべく**違う領域**にまたがらせて、1日の中でも広さを出す。

## 設定（出力先）— クラウド実行（GitHubリポジトリ）
- このスキルは **クラウドのルーチン** として、リポジトリ `takuma0403/daily-tech-curio` を毎回クローンして実行される。
- **記録＝メモリはこのクローンしたリポジトリの中だけにある**。ローカルマシンや `.env` は存在しない。状態は必ずリポジトリのファイルから読み、リポジトリへ書き戻すこと。
- 出力先 = クローンしたリポジトリのルート。
  - 日次ファイル: `daily/YYYY-MM-DD.md`
  - インデックス: `index.md`
  - どちらも無ければ新規作成する（テンプレートは「記録の仕組み」を参照）。
- このリポジトリのデフォルトブランチは `claude/notebook`。**作業中のブランチ（＝デフォルトブランチ）にそのままコミットしてpushする**。新しいブランチを切らない。PRも作らない。

## 実行手順（毎日同じ）
1. **今日の日付・曜日を確定**する。
2. **重複回避**: クローンしたリポジトリ内の `index.md` と直近10日分の `daily/*.md` を読み、これまで扱った「技術名・仕様名」を一覧化する。今日はそれらと被らないものを選ぶ。被ったら選び直す（実行ごとに記憶はリセットされるので、このリポジトリの読み込みが唯一の重複防止策。必ず行う）。
3. **領域を決める**: 下のローテ表で今日の曜日の「アンカー領域」を取る。3枚のうち最低1枚はそこから出す。残り2枚は、日付シード（例: 日にち＋曜日の合計）で表の**別の領域**から選び、3枚がなるべく違う領域になるようにする。
4. **moveを3つ選ぶ**: 「発想エンジン」から、3枚それぞれ**異なるmove**を割り当てる（日付シードでローテ）。
5. **素材を掘る（Web検索）**: 各領域で「あまり知られていない仕様・機能・転用」を探す。トレンドの表面ではなく長尾を狙う。検索例:
   - `lesser-known {領域} spec` / `{protocol} hidden feature` / `undocumented {hardware}` / `creative misuse of {API}` / `{tech} unintended use` / RFC errata
   - 「本来どこで使われてるか」の裏取りも検索で行い、**出典URLを必ず残す**。
6. **レンズを1つ選ぶ**: 日付シードで下のレンズリストから1つ。3枚のうち「奇抜さ」を担う使い方に効かせる。
7. **発想（多産→選抜）**: 各カードについて、いきなり1個を作り込まず、まず雑にタネを3〜5個出す。採否ルーブリックで最良の1つに絞る。最頻解（凡庸キル・リスト該当）は破棄して別方向へ振り直す。
8. **出力フォーマットで3枚書く**（下記）。温度・文体・厚みは `references/examples.md` のお手本に合わせる。
9. **記録する**:
   - `daily/YYYY-MM-DD.md` を新規作成し、冒頭に「今日の3枚」目次（各カードへのアンカーリンク）→ 3枚を書き込む。
   - `index.md` の見出し直下に今日のエントリ（日付・領域・レンズ・全文リンク・3枚のタイトルリンク＋各1行フック）を**先頭に追記**する（最新が上）。
10. **コミットして push する**（これをしないと記録が残らず、翌日の重複回避も効かない）:
    ```
    git add daily/ index.md
    git commit -m "ideas: YYYY-MM-DD"
    git push origin HEAD
    ```
    - 今いるブランチ（デフォルトブランチ `claude/notebook`）にそのまま push する。別ブランチを切らない。push が失敗したら、その回の出力をログに残して次回に持ち越す。

## ローテーション

### アンカー領域（曜日）
- **月**: フロントエンド/グラフィックス（WebGPU, Canvas, WebCodecs, OpenTypeフォント）
- **火**: ネットワーク/プロトコル（HTTP/3, WebTransport, WebRTC, mDNS/DNS-SD）
- **水**: 無線/組み込み/ハードウェア（BLE, NFC, UWB, WebSerial/WebUSB, 各種センサー）
- **木**: セキュリティ/認証（WebAuthn/Passkeys, OAuth/OIDC, mTLS, attestation）
- **金**: データ/同期/ストレージ（CRDT, SQLite WASM, IndexedDB, ローカルファースト）
- **土**: AI/メディア処理（オンデバイスML, WebNN, 音声/DSP, 画像処理）
- **日**: ワイルドカード（上記以外。電力網・物理現象・生物・金融・歴史的プロトコルなど何でも）

### レンズ（日にち mod 7 などで決定的に回す）
`オフライン完結` / `サーバー不要` / `マイコン1個で動く` / `物理世界とつながる` / `無関係な2技術の組み合わせ` / `既存サービスの1機能だけを置換` / `5分で動くデモにできる`

## 発想エンジン（generative moves）— 3枚に別々に割り当てる
- **仕様の目的外利用**（本来の用途と違うことに使う）
- **サイドチャネル/副作用の利用**（タイミング・消費電力・電波の乱れ・センサーのクロストーク）
- **レガシー/廃止予定機能の蘇生**
- **センサーの転用**（加速度計→マイク、カメラ→分光器、磁気計→…）
- **物理現象へのピギーバック**（超音波・画面のPWM点滅・共振・既存の巨大ネットワークへの便乗）
- **人間/身体をプロトコルの一部にする**
- **異分野の手法移植**（生物・金融・音楽の手法をシステムへ）
- **制約の極端化**（GPIO1本だけ・サーバー0・1ファイルで）

## 凡庸キル・リスト（該当したら破棄して、もっと変な方へ再生成）
`〜のAI版` / `〜のUber` / `ブロックチェーンで〜` / `ダッシュボード` / `ToDo・メモアプリ` / `チャットボット` / `単なるAPIラッパー` / `「〜を便利にする」だけ` / 仕組みの肝が「AIで」に逃げている

## 採否ルーブリック（全部YESでないと不採用）
- 仕組みに、**名前のついた具体的でややマイナーな技術**が1つ以上効いているか？（“AIで”で逃げていない）
- 思わず「なんでそんなことを？」と言いたくなるか？（実用一辺倒は落第）
- 「そんな手があったか」＝仕組みに一捻りあるか？
- 凡庸キル・リストに当たっていないか？
- 読み手が自力では思いつかなそうな入口か？

## 出力フォーマット（各カード）

```
### <a id="card-1"></a>{絵文字} {キャッチーな見出し} 〔move: {move} / 領域: {領域}〕

**事実**: {その技術/仕様が何で、なぜ見落とされがちか。2〜4文}

**何ができる**: {それで実現できること。具体的に、1つ踏み込んで}

**技術の魅力**: {なぜ面白いか、どこが効いているか。非対称性・意外性の核を言語化}

**本来どこで使われてるか**: {実際の用途・実例。Web検索で裏取り。出典URLを文末か脚注に}

🔎 検索ワード: `英語の固有名詞クエリ` / `沼に入るクエリ` / `別角度のクエリ` / 「日本語クエリ」
```

- 見出しはキャッチーに。ただし本文は説明調を保つ（小説風にしすぎない）。
- 「本来どこで使われてるか」は2〜4文で、実例を1つは具体的に。
- 🔎は3〜4個。**英語の固有名詞クエリ**（ヒットしやすい）＋日本語を1つ。

## 記録の仕組み（Markdown）

### `daily/YYYY-MM-DD.md`（その日の全文）
```
# {YYYY-MM-DD} ({曜日}) — 領域: {3枚の領域} · レンズ: {lens}

> **今日の3枚**: [{見出し1}](#card-1) / [{見出し2}](#card-2) / [{見出し3}](#card-3)

{カード1（フォーマット通り、id=card-1）}

---

{カード2（id=card-2）}

---

{カード3（id=card-3）}
```

### `index.md`（週を見渡す一覧 / 最新が上）
見出しの直下に、毎日この1ブロックを**先頭へ追記**する:
```
## {YYYY-MM-DD} ({曜日}) · 領域: {…} · レンズ: {lens}
[全文 →](daily/{YYYY-MM-DD}.md)
- 🆕 [{見出し1}](daily/{YYYY-MM-DD}.md#card-1) — {1行フック}
- 🔍 [{見出し2}](daily/{YYYY-MM-DD}.md#card-2) — {1行フック}
- 💡 [{見出し3}](daily/{YYYY-MM-DD}.md#card-3) — {1行フック}

---
```
- ファイルが無ければ、`index.md` は見出し（`# 技術知見デイリー — 記録インデックス` ＋説明1行）だけ作ってから追記する。
- 直近7日分を上に見れば「一週間分」が一望できる。古い分はそのまま下に積み上がる。

## 良い例（目標温度）
`references/examples.md` に ENF / mDNS / GSUBフォント の3枚を収録。**新規生成時は、この温度・文体・厚み・🔎の付け方に合わせる**こと。お手本より薄い・説明的すぎる・凡庸、になっていないか毎回照合する。
